<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AtmiyaInnovation" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorPrimary">@color/atmiya_primary</item>
        <item name="android:colorPrimaryDark">@color/atmiya_primary</item>
        <item name="android:colorAccent">@color/atmiya_secondary</item>
        
        <!-- Splash Screen Configuration -->
        <item name="android:windowBackground">@color/soft_bg_light</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
    </style>
</resources>
